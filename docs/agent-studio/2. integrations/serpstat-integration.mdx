# Serpstat

Serpstat is a comprehensive SEO and digital marketing platform that provides keyword research, competitor analysis, and site audit capabilities. It helps marketers and SEO professionals analyze search data, track competitors, and optimize their online presence. The platform offers extensive data analytics and insights for improving search engine rankings and digital marketing strategies.

At `SmythOS`, we have fully integrated `Serpstat` capabilities within our platform, providing custom components that facilitate advanced SEO research and competitive analysis. This integration allows users to access powerful keyword research tools, competitor analysis features, and detailed search analytics directly within their workflows.

## ⚙️ Settings

#### 🔐 Authentication Details
- Sign up for a Serpstat account
- Obtain your API token from your Serpstat account settings  
- Store the API token securely in your environment variables
- Use the token for authentication in all API requests

## Search Suggestions

Provides search suggestions for the keyword you requested through full-text search functionality.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keyword (`required<string>`)**: The search term to analyze
- **filters (`optional<object>`)**: Additional filtering parameters
- **se (`optional<string>`)**: Search engine and region (default: g_us)
- **page (`optional<integer>`)**: Page number for pagination (default: 1)
- **size (`optional<integer>`)**: Number of results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Search suggestion results
- **page**: Current page number
- **total**: Total number of results
- **left_lines**: Remaining available requests

## Keywords Selection

Exports organic keywords associated with the researched keyword to a CSV file.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keyword (`required<string>`)**: The keyword to analyze
- **se (`optional<string>`)**: Search engine and region (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)
- **sort (`optional<string>`)**: Sort direction (default: asc)
- **filters (`optional<object>`)**: Additional filtering parameters

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers

## Related Keywords

Retrieves related keywords with their metrics and search intent data.

### Inputs
- **search_question (`required<string>`)**: The keyword to find related terms for
- **search_country (`required<string>`)**: Target country for search data

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **keyword**: Related keyword
- **volume**: Search volume
- **cpc**: Cost per click
- **search-intent**: User intent classification
- **competition_value**: Competition level

## Domain Ads

Returns paid keywords and ad copies for a specific domain.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain to analyze
- **se (`required<string>`)**: Search engine and region (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Ad data results
- **page**: Current page number
- **left_lines**: Remaining available requests

## Ads Examples

Retrieves paid keywords and ad copies for specific keywords.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keyword (`required<string>`)**: Keyword to analyze
- **se (`required<string>`)**: Search engine and region
- **domains (`optional<array>`)**: List of domains to filter
- **page (`optional<integer>`)**: Page number
- **filters (`optional<object>`)**: Additional filtering parameters
- **size (`optional<integer>`)**: Results per page

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Ad examples data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total number of results

## Top Pages By Domain

Returns website pages in hierarchical order.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain
- **se (`optional<string>`)**: Search engine and region (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)
- **filters (`optional<object>`)**: Additional filtering parameters

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Page hierarchy data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total number of results

## Export Search Questions And Suggestions

Exports search suggestions and questions data.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keyword (`required<string>`)**: Search term to analyze
- **se (`required<string>`)**: Search engine and region (default: g_us)
- **filters (`optional<object>`)**: Additional filtering parameters
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **withQuestions (`optional<boolean>`)**: Include questions in results

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers

## URL Rank Keywords

Returns keywords that a URL ranks for in Google top-100 search results.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **se (`required<string>`)**: Search engine and region (default: g_us)
- **url (`required<string>`)**: Target URL to analyze
- **filters (`optional<object>`)**: Additional filtering parameters
- **sort (`optional<string>`)**: Sort field (default: position)
- **order (`optional<string>`)**: Sort order (default: asc)
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Ranking keywords data
- **page**: Current page number
- **total**: Total number of results
- **left_lines**: Remaining available requests

## URL Competitors

Returns the list of URL competitors.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **se (`required<string>`)**: Search engine and region (default: g_us)
- **url (`required<string>`)**: Target URL to analyze
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Competitor data
- **page**: Current page number
- **total**: Total number of results
- **left_lines**: Remaining available requests

## Missing Keywords

Returns keywords that competitors rank for but the given URL doesn't.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **se (`required<string>`)**: Search engine and region (default: g_us)
- **url (`required<string>`)**: Target URL to analyze
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **filters (`optional<object>`)**: Additional filtering parameters

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Missing keywords data
- **page**: Current page number
- **left_lines**: Remaining available requests

## Traffic By URL Mask

Returns information about traffic and keywords number by URL mask.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **se (`required<string>`)**: Search engine and region (default: g_us)
- **domain (`required<string>`)**: Target domain to analyze
- **urlContains (`optional<string>`)**: URL mask filter
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **urls**: List of matching URLs
- **traffic**: Traffic data
- **left_lines**: Remaining available requests

## Summary Report

Gives you general information about link profile.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Summary report data
- **left_lines**: Remaining available requests

## Backlinks Dynamics

Returns the history of found and lost backlinks for the analyzed site.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **query (`required<string>`)**: Search query
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Backlinks history data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Total count of results

## Domain Rank Distribution

Returns an array containing objects with SDR value ranges and the number of domains that are included in these ranges.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Domain rank distribution data
- **left_lines**: Remaining available requests

## TOP-10 Anchors

Returns information about the TOP-10 anchors.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Anchor text data
- **left_lines**: Remaining available requests
- **referring_domains**: Number of referring domains
- **backlinks**: Number of backlinks
- **unique_anchors**: Number of unique anchors

## Top Level Domains Distribution

Returns information about the number of domain backlinks that belong to a specific top-level domain zone.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain
- **searchType (`required<string>`)**: Type of search to perform
- **lang (`required<string>`)**: Language filter
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: TLD distribution data
- **left_lines**: Remaining available requests
- **tld_count**: Count of top-level domains
- **domains_count**: Total domains count
- **biggest_tld_percentage**: Percentage of largest TLD

## Referring Domains

List of referring domains of a site you analyze.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **query (`required<string>`)**: Search query
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Referring domains data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available

## Malicious Domains Donors

List of malicious domains referring to the analyzed domain and information on them.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **query (`required<string>`)**: Search query
- **searchType (`optional<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **linkPerDomain (`optional<integer>`)**: Links per domain limit

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Malicious domains data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## Malicious Pages Donors

Returns a list of links from malicious sites referring to the analyzed domain.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **linkPerDomain (`optional<integer>`)**: Links per domain limit (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Malicious pages data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## Malicious Domains Acceptors

Returns a list of malicious domains referring to the analyzed domain and information on them.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **searchType (`optional<string>`)**: Type of search to perform (default: domain)
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options (default: [])
- **linkPerDomain (`optional<integer>`)**: Links per domain limit (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Malicious domains data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## Active Backlinks

Gives you a list of active backlinks of a site you analyze.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **query (`required<string>`)**: Search query
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **linkPerDomain (`optional<integer>`)**: Links per domain limit (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Active backlinks data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## Lost Backlinks

Gives you a list of lost backlinks of a site you analyze.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **searchType (`optional<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **linkPerDomain (`optional<integer>`)**: Links per domain limit

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Lost backlinks data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## External Domains

Gives you a list of the domains that are linked to the analyzed site.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **query (`required<string>`)**: Search query
- **searchType (`optional<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **linkPerDomain (`optional<integer>`)**: Links per domain limit

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: External domains data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## External Lost Links

Gives you a list of lost external links of a site you analyze.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **linkPerDomain (`optional<integer>`)**: Links per domain limit

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Lost external links data
- **left_lines**: Remaining available requests
- **left_page**: Remaining pages
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## External Active Links

List of external backlinks of a site you analyze.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **linkPerDomain (`optional<integer>`)**: Links per domain limit (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Active external links data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## Anchors

Gives you a list of keywords used as anchors for backlinks of a site you analyze.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **query (`required<string>`)**: Search query
- **searchType (`optional<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **complexFilter (`optional<object>`)**: Advanced filtering options
- **count (`optional<integer>`)**: Number of results (default: 1)
- **anchor (`optional<string>`)**: Specific anchor text to search for

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Anchor text data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## Top Pages (Backlink)

Gives you a list of leading pages of the analyzed site by the number of backlinks with referring pages, domains, and IPs.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **searchType (`required<string>`)**: Type of search to perform
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page
- **complexFilter (`optional<object>`)**: Advanced filtering options

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Top pages data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## Links Intersection Summary

Gives you a list of the number of common domains (donors) that refer to the analyzed domain.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **intersect (`required<string>`)**: Intersection criteria
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Intersection summary data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available

## Links Intersection Detailing

Gives you a list of backlinks from intersecting donors to the analyzed domain.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **intersect (`required<string>`)**: Intersection criteria
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page
- **complexFilter (`optional<object>`)**: Advanced filtering options

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Detailed intersection data
- **left_lines**: Remaining available requests
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order

## List Audits

Provides you with a list of audits carried out by project id.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **limit (`optional<integer>`)**: Maximum number of results
- **offset (`optional<integer>`)**: Number of results to skip
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result**: Audit list data

## Get Project Settings

Returns a list of settings in a specific project.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **projectId (`required<string>`)**: Project identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result**: Project settings data

## Audit Settings Saving

Saves settings in a suggested project.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **mainSettings (`required<object>`)**: Main configuration settings
- **dontScanKeywordsBlock (`optional<object>`)**: Keywords to exclude from scan
- **onlyScanKeywordsBlock (`optional<object>`)**: Keywords to include in scan
- **baseAuthBlock (`optional<object>`)**: Authentication configuration
- **mailTriggerSettings (`optional<object>`)**: Email notification settings
- **scheduleSettings (`optional<object>`)**: Schedule configuration
- **scanSetting (`optional<object>`)**: Scan parameters
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result**: Settings save result

## Get Project Default Settings

Introduces a list of default settings values in a project.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result**: Default settings data

## Audit Start

Starts an audit due to the settings saved previously.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **projectId (`required<string>`)**: Project identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **reportId**: Audit report identifier

## Stop Running Audit

Stops a running audit.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result**: Stop operation result

## Audit Basic Info

Returns basic report information.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **reportId (`required<string>`)**: Report identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **reportId**: Report identifier
- **date**: Report date
- **sdo**: SDO value
- **highCount**: Number of high priority issues
- **mediumCount**: Number of medium priority issues
- **lowCount**: Number of low priority issues
- **informationCount**: Number of information messages
- **virusesCount**: Number of viruses detected
- **progress**: Audit progress
- **stoped**: Audit stop status
- **specialStopReason**: Reason for special stop
- **checkedPageCount**: Number of pages checked
- **totalCheckedPageCount**: Total pages checked
- **redirectCount**: Number of redirects
- **captchaDetected**: Captcha detection status

## List Report Issues

Returns the number of errors and information messages found by issues categories in a specific report.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **reportId (`required<string>`)**: Report identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_categories**: Categories of issues found

## Issue Elements Data

Provides you with a list of elements which contains a specific error or information message.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`optional<string>`)**: Project identifier
- **reportId (`optional<string>`)**: Report identifier
- **errorName (`optional<string>`)**: Specific error name
- **mode (`optional<string>`)**: Operation mode
- **limit (`optional<integer>`)**: Maximum number of results
- **offset (`optional<integer>`)**: Number of results to skip
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **compareReportId (`optional<string>`)**: Report ID for comparison

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Issue elements data

## List Project Regions

Returns the list of project regions and their status.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **projectId**: Project identifier
- **regions**: List of regions
- **spent_limits**: Usage limits information

## List Rank Tracker Projects

Get a list of your projects.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **page (`optional<integer>`)**: Page number (default: 1)
- **pageSize (`optional<integer>`)**: Results per page (default: 20)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Projects data
- **page**: Current page number
- **count**: Results count
- **total**: Total results available
- **page_total**: Total number of pages

## Site Audit Credits Stats

Displays the statistics of Site Audit credits.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **hasAuditOnePage**: Single page audit status
- **total**: Total credits
- **used**: Used credits
- **left**: Remaining credits

## Summary Report Export

Generates an exporting file in a summary report.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **reportId (`required<string>`)**: Report identifier
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **urls**: Export file URLs

## URL List For Scanning

Returns a list of URLs which were audited if the scan type was specified for assigned URLs.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **projectId (`required<string>`)**: Project identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **urls**: List of URLs
- **scanType**: Type of scan performed
- **isImported**: Import status

## Issue Categories Statistics

Brings in statistics by issue categories.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **reportId (`required<string>`)**: Report identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result**: Statistics data

## Project Changes By Issues Categories

Shows the number of changed errors and information messages by a specific issues category in every report of a project.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **projectId (`required<string>`)**: Project identifier
- **errorName (`optional<string>`)**: Specific error name
- **limit (`optional<integer>`)**: Maximum number of results
- **offset (`optional<integer>`)**: Number of results to skip
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **errorCounts_result**: Error count changes data

## List Team Users

Returns list of all users in your team.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Team users data
- **total**: Total number of users
- **page**: Current page number
- **limits**: Usage limits

## Deactivate Team User

Deactivates active user in your team.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **user_id (`required<string>`)**: User identifier to deactivate

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **success**: Operation success status
- **email**: User email
- **user_id**: User identifier
- **limits**: Usage limits

## Activate Team User

Activates inactive user in your team.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **user_id (`required<string>`)**: User identifier to activate

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **success**: Operation success status
- **user_id**: User identifier
- **limits**: Usage limits

## Remove User From Team

Removes existing user from your team.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **user_id (`required<string>`)**: User identifier to remove
- **merge_projects (`optional<boolean>`)**: Whether to merge user's projects

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **success**: Operation success status
- **user_id**: User identifier
- **limits**: Usage limits

## Add User To Team

Invites new user to join your team.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **email (`required<string>`)**: Email address of user to invite
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **success**: Operation success status
- **email**: Invited user's email
- **user_id**: User identifier
- **limits**: Usage limits

## Last Reports

Returns information about generated reports and links to download them.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **filters (`optional<object>`)**: Filtering parameters (default: {})
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Reports data
- **left_lines**: Remaining available requests
- **total_reports**: Total number of reports
- **page**: Current page number
- **size**: Results per page
- **sort**: Sort criteria

## Check Account Balance

Returns balance on the account, used for SERP and volume parsing.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **available_usd**: Available balance in USD
- **keywordsStatusCount**: Keywords status count
- **serp_parsing_cost**: SERP parsing cost
- **volume_parsing_cost**: Volume parsing cost

## Check Task Status

Obtain a position's check status with 'task_id'.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **taskId (`required<string>`)**: Task identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **totalKeywords**: Total number of keywords
- **keywordsStatusCount**: Keywords status count
- **progress**: Task progress status

## Send Keywords to SVCS

Sends keywords to volume parsing.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keywords (`required<array>`)**: List of keywords to parse
- **typeId (`optional<string>`)**: Type identifier
- **selId (`optional<string>`)**: Selection identifier
- **regionId (`optional<string>`)**: Region identifier

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **taskId**: Task identifier

## Get HTML RAW SERP

Returns the raw SERP in HTML format using task ID and keyword ID.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **taskId (`required<string>`)**: Task identifier
- **keywordId (`required<string>`)**: Keyword identifier
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **date**: SERP date
- **serp**: Raw SERP HTML

## Issue Subelements Data

Returns a list of subelements which contains a specific error or information message.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **reportId (`required<string>`)**: Report identifier
- **errorName (`optional<string>`)**: Specific error name
- **mode (`optional<string>`)**: Operation mode
- **limit (`optional<integer>`)**: Maximum number of results
- **offset (`optional<integer>`)**: Number of results to skip
- **crc (`optional<string>`)**: CRC value
- **compareReportId (`optional<string>`)**: Report ID for comparison

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Subelements data
- **totalCount**: Total count of subelements

## List Top 20 Project Keywords Domains

Returns the list of domains for the required region, which were listed in the top-20 for any project keywords.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **projectRegionId (`required<string>`)**: Project region identifier
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page
- **dateFrom (`optional<string>`)**: Start date filter
- **dateTo (`optional<string>`)**: End date filter
- **domains (`optional<array>`)**: List of domains to filter

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **projectId**: Project identifier
- **projectRegionId**: Project region identifier
- **competitors**: Competitor domains data
- **page**: Current page number
- **page_total**: Total number of pages
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order
- **spent_limits**: Usage limits information

## List Pages And Positions By Domain

Returns the history of the positions and urls for the keywords to the requested region.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **projectRegionId (`required<string>`)**: Project region identifier
- **domain (`required<string>`)**: Target domain
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page
- **dateFrom (`optional<string>`)**: Start date filter
- **dateTo (`optional<string>`)**: End date filter
- **keywords (`optional<array>`)**: List of keywords to filter
- **withTags (`optional<boolean>`)**: Include tags in results

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **projectId**: Project identifier
- **projectRegionId**: Project region identifier
- **domain**: Domain analyzed
- **page**: Current page number
- **page_total**: Total number of pages
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order
- **spent_limits**: Usage limits information
- **keywords**: Keywords data

## Get SERP History By Keywords

Returns Google's top-100 search results for the required region and project keywords.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **projectId (`required<string>`)**: Project identifier
- **projectRegionId (`required<string>`)**: Project region identifier
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page
- **dateFrom (`optional<string>`)**: Start date filter
- **dateTo (`optional<string>`)**: End date filter
- **sort (`optional<string>`)**: Sort field
- **order (`optional<string>`)**: Sort order
- **keywords (`optional<array>`)**: List of keywords to filter
- **withTags (`optional<boolean>`)**: Include tags in results

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **projectId**: Project identifier
- **projectRegionId**: Project region identifier
- **page**: Current page number
- **page_total**: Total number of pages
- **count**: Results count
- **total**: Total results available
- **sort**: Sort field
- **order**: Sort order
- **keywords**: Keywords data

## Keywords SERP Crawl

Sends keywords with commas to crawl regular SERP and local results.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keywords (`required<array>`)**: Keywords to crawl
- **typeId (`optional<string>`)**: Type identifier
- **selId (`optional<string>`)**: Selection identifier
- **countryId (`optional<string>`)**: Country identifier
- **langId (`optional<string>`)**: Language identifier

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **task_id**: Task identifier

## Get SERP Results

Gets a SERP Crawling of regular and local results using the identifiers.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **taskId (`required<string>`)**: Task identifier

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **task_meta**: Task metadata
- **result_tops**: SERP results data

## Domain History

Provides you with the historical data on a domain's number of keywords and visibility.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain
- **se (`required<string>`)**: Search engine
- **size (`optional<integer>`)**: Results per page
- **page (`optional<integer>`)**: Page number
- **filters (`optional<object>`)**: Additional filtering parameters
- **during_all_time (`optional<boolean>`)**: Include all historical data

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Historical data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total results available

## Domain Summary

Provides the summary information for an array of domains.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domains (`required<array>`)**: List of domains to analyze
- **se (`required<string>`)**: Search engine (default: g_us)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Domain summary data

## Domain Keywords And Positions

Exports to .csv file domains positions and keywords.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **se (`optional<string>`)**: Search engine (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 6000)
- **page (`optional<integer>`)**: Page number (default: 1)
- **filters (`optional<object>`)**: Additional filtering parameters (default: {})

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers

## Domain Keywords

Keywords which the analyzed domain ranks for in Google top-100 search results.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain
- **se (`optional<string>`)**: Search engine
- **size (`optional<integer>`)**: Results per page
- **page (`optional<integer>`)**: Page number
- **filters (`optional<object>`)**: Additional filtering parameters
- **keywords (`optional<array>`)**: Keywords to filter
- **minusKeywords (`optional<array>`)**: Keywords to exclude
- **withSubdomains (`optional<boolean>`)**: Include subdomains

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Keywords data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total results available

## Competitors in OSERP

List of domain competitors in top 20 Google search results.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain
- **se (`optional<string>`)**: Search engine (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Competitors data
- **page**: Current page number
- **left_lines**: Remaining available requests

## Paid SERP Competitors By Keyword

List of competitors in PPC for the given keyword.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **query (`required<string>`)**: Search query
- **se (`required<string>`)**: Search engine (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Competitors data
- **page**: Current page number
- **left_lines**: Remaining available requests

## Top Pages By Keyword

Returns website pages that rank for the largest amount of the analyzed keyword variations and have the highest traffic.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keyword (`required<string>`)**: Keyword to analyze
- **se (`optional<string>`)**: Search engine (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_urls**: URLs data
- **page**: Current page number
- **page_size**: Results per page
- **order**: Sort order
- **sort**: Sort field
- **left_lines**: Remaining available requests
- **keyword**: Analyzed keyword
- **se**: Search engine used

## Domain VS Domain

Returns common keywords of up to 3 domains.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domains (`required<array>`)**: List of domains to compare
- **se (`required<string>`)**: Search engine (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 1000)
- **page (`optional<integer>`)**: Page number (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Comparison data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total results available

## Domains' Unique Keywords

Returns unique keywords of two domains that the third domain does not rank for.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domains (`required<array>`)**: List of domains to analyze
- **se (`required<string>`)**: Search engine
- **minusDomain (`required<string>`)**: Domain to exclude
- **size (`optional<integer>`)**: Results per page
- **page (`optional<integer>`)**: Page number
- **filters (`optional<object>`)**: Additional filtering parameters

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Unique keywords data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total results available

## Domain Ads

Returns paid keywords and ads copies that pop up for the queried domain in paid search results.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **se (`required<string>`)**: Search engine (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)
- **filters (`optional<object>`)**: Additional filtering parameters (default: {})
- **keywords (`optional<array>`)**: Keywords to filter (default: [])
- **minusKeywords (`optional<array>`)**: Keywords to exclude (default: [])

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Ads data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total results available

## List Domain URLs

Returns the list of URLs within the analyzed domain and the number of keywords for each URL.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **se (`optional<string>`)**: Search engine (default: g_us)
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)
- **filters (`optional<object>`)**: Additional filtering parameters (default: {})

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: URLs data
- **page**: Current page number
- **left_lines**: Remaining available requests
- **total**: Total results available

## Domain Traffic In Serpstat db

Returns an array with information about the amount of traffic by domain in Serpstat databases.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **domain (`required<string>`)**: Target domain
- **size (`optional<integer>`)**: Results per page (default: 100)
- **page (`optional<integer>`)**: Page number (default: 1)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Traffic data

## Regional Domain Keywords Number

Returns an array with information about the number of keys per domain in Serpstat databases.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Keywords data
- **analysed_domain**: Analyzed domain
- **sort**: Sort field
- **order**: Sort order
- **regions_db_count**: Number of region databases
- **total_keywords**: Total keywords count
- **left_lines**: Remaining available requests

## List Serpstat Projects

Get a list of your Serpstat projects.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **page (`optional<integer>`)**: Page number
- **size (`optional<integer>`)**: Results per page

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Projects data
- **page**: Current page number
- **page_total**: Total number of pages
- **count**: Results count
- **total**: Total results available

## Create Serpstat Project

Create a new project in Serpstat.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **domain (`required<string>`)**: Target domain
- **name (`optional<string>`)**: Project name
- **group_name (`optional<string>`)**: Group name
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **project_id**: Created project identifier

## Delete Serpstat Project

Delete your Serpstat project.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request (default: 1)
- **project_id (`required<string>`)**: Project identifier to delete
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result**: Delete operation result

## Keyword Overview

Provides you with the keyword overview.

### Inputs
- **id (`required<string>`)**: Unique identifier for the request
- **keywords (`required<array>`)**: Keywords to analyze
- **se (`optional<string>`)**: Search engine (default: g_us)
- **filters (`optional<object>`)**: Additional filtering parameters
- **page (`optional<integer>`)**: Page number (default: 1)
- **size (`optional<integer>`)**: Results per page (default: 100)

### Outputs
- **Response**: Complete API response
- **Headers**: Response headers
- **id**: Request identifier
- **result_data**: Keyword overview data
- **page**: Current page number
- **left_lines**: Remaining available requests

