# OneDrive Integration 🌐

Microsoft OneDrive is a versatile cloud storage service that allows users to store files, share documents, and collaborate in real-time. The OneDrive API offers developers seamless access to OneDrive functionality, enabling integration into various applications, making it an essential tool for modern digital workflows.

Integrating the OneDrive API into SmythOS significantly enhances the platform's capabilities by:
- **Improving Collaboration:** Facilitating seamless file sharing and collaborative editing among team members.
- **Boosting Productivity:** Allowing users to access and manage files from anywhere, boosting overall productivity.
- **Centralizing Data Management:** Providing a unified platform to manage files and documents effectively.
- **Ensuring Security:** Offering robust security features to protect sensitive data in compliance with organizational policies.

## List Drives

Retrieve a user's drives from OneDrive.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot1.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following input:

1. **userId (`required<string>`):** The ID of the user whose drives you want to list.

<Image src="/img/one_drive/screenshot2.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **drives:** The list of drives associated with the user.

<Image src="/img/one_drive/screenshot3.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## Get User OneDrive

Retrieve a user's default OneDrive.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot4.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following input:

1. **userId (`required<string>`):** The ID of the user whose default OneDrive you want to retrieve. 

<Image src="/img/one_drive/screenshot5.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **driveId:** The ID of the user's default OneDrive.

<Image src="/img/one_drive/screenshot6.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## Get Drive

Retrieve a drive by ID from OneDrive.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot7.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following input:

1. **driveId (`required<string>`):** The ID of the drive you want to retrieve. 

<Image src="/img/one_drive/screenshot8.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **driveId:** The ID of the retrieved drive.
4. **name:** The name of the retrieved drive.

<Image src="/img/one_drive/screenshot9.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## Get Item by ID

Retrieve a user's OneDrive item by its ID.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot10.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following inputs:

1. **userId (`required<string>`):** The ID of the user who owns the item.
2. **itemId (`required<string>`):** The ID of the item you want to retrieve.  

<Image src="/img/one_drive/screenshot11.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **itemId:** The ID of the retrieved item.
4. **name:** The name of the retrieved item.
5. **webUrl:** The URL to access the retrieved item.

<Image src="/img/one_drive/screenshot12.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## Get Item by Path

Retrieve a user's OneDrive item by its path.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot13.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following inputs:

1. **userId (`required<string>`):** The ID of the user who owns the item.
2. **item-path (`required<string>`):** The path of the item you want to retrieve.    

<Image src="/img/one_drive/screenshot14.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **itemId:** The ID of the retrieved item.
4. **name:** The name of the retrieved item.
5. **webUrl:** The URL to access the retrieved item.

<Image src="/img/one_drive/screenshot15.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## List Drive Item Children

Retrieve a user's OneDrive item children.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot16.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following inputs:

1. **userId (`required<string>`):** The ID of the user who owns the item.
2. **itemId (`required<string>`):** The ID of the item whose children you want to list.    

<Image src="/img/one_drive/screenshot17.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **children:** The list of child items of the specified item.

<Image src="/img/one_drive/screenshot18.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## Get Group's Document Library

Retrieve a group's document library.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot19.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following input:

1. **groupId (`required<string>`):** The ID of the group whose document library you want to retrieve. 

<Image src="/img/one_drive/screenshot20.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **driveId:** The ID of the retrieved drive.

<Image src="/img/one_drive/screenshot21.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## List Group Drives

Retrieve a group's drives from OneDrive.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot22.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following input:

1. **groupId (`required<string>`):** The ID of the group whose drives you want to list.

<Image src="/img/one_drive/screenshot23.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **drives:** The list of drives associated with the group.

<Image src="/img/one_drive/screenshot24.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## Get Group's Item By ID

Retrieve a group's drive item by ID.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot25.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following inputs:

1. **groupId (`required<string>`):** The ID of the group whose drive item you want to retrieve.
2. **itemId (`required<string>`):** The ID of the drive item you want to retrieve.

<Image src="/img/one_drive/screenshot26.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **itemId:** The ID of the retrieved item.
4. **name:** The name of the retrieved item.
5. **webUrl:** The URL to access the retrieved item. 

<Image src="/img/one_drive/screenshot27.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## Get Group's Item By Path

Retrieve a group's OneDrive item by its path.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot28.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following inputs:

1. **groupId (`required<string>`):** The ID of the group whose drive item you want to retrieve.
2. **item-path (`required<string>`):** The path of the drive item you want to retrieve. 

<Image src="/img/one_drive/screenshot29.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **itemId:** The ID of the retrieved item.
4. **name:** The name of the retrieved item.
5. **webUrl:** The URL to access the retrieved item.

<Image src="/img/one_drive/screenshot30.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## List Group's Item Children

Retrieve a group's OneDrive item children.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot31.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following inputs:

1. **groupId (`required<string>`):** The ID of the group whose drive item you want to retrieve.
2. **itemId (`required<string>`):** The ID of the item whose children you want to retrieve.

<Image src="/img/one_drive/screenshot32.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **children:** A list of the child items of the specified item.

<Image src="/img/one_drive/screenshot33.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.

## List Root Items

Fetches a user's OneDrive root items.

### 🛠 Settings

#### Client ID & Client Secret

To kickstart this component, follow these initial steps to obtain your `Client ID` and `Client Secret` using Microsoft Entra:

1. **Register Your Application:**
   - Go to the [Microsoft Entra Portal](https://entra.microsoft.com/).
   - Navigate to **Azure Active Directory** and select **App registrations**.
   - Click on **New registration** to create a new application.

2. **Provide Application Details:**
   - **Name:** Enter a name for your application.
   - **Supported account types:** Choose the account types that you want to support. This can be:
     - Accounts in this organizational directory only.
     - Accounts in any organizational directory.
     - Accounts in any organizational directory and personal Microsoft accounts.
   - **Redirect URI:** Select `Web` and enter `https://app.smythos.com/oauth/oauth2/callback` as the URI.
   - Click on **Register**.

3. **Obtain Client ID & Client Secret:**
   - **Client ID:** Once registered, go to the **Overview** section of your new application to find your `Client ID` (also known as the Application ID). Copy this value.
   - **Client Secret:**
     - Navigate to **Certificates & secrets**.
     - In the **Client secrets** section, click on **New client secret**.
     - Provide a description and choose an expiration period.
     - Click on **Add**.
     - Copy the client secret value displayed. This is your `Client Secret`. Ensure you copy it now because it will be hidden once you leave the page.

4. **Configure API Permissions:**
   - **API Permissions:** Ensure the necessary permissions to use OneDrive are configured. This usually includes granting permissions like `Files.Read.All` and `Files.ReadWrite.All`. For detailed steps, refer to the Microsoft documentation on configuring API permissions.

5. **Enter Credentials in SmythOS:**
   - Navigate to the settings section of the component in SmythOS.
   - Enter the `Client ID` and `Client Secret` that you obtained from the Microsoft Entra portal.
   - Click on the **Authenticate** button to connect the component to OneDrive.

> **Note:** Treat the `Client ID` & `Client Secret` as sensitive data. In `SmythOS`, you can securely manage it using the `Vault` feature. For more details, please refer to [this](https://app.smythos.com/doc/#/vault/keysManagement) section in the documentation.

#### 🔑 Authenticate/Sign Out Button

The component provides a versatile button designed to toggle between **Authenticate** and **Sign Out** based on the current authentication state within the OAuth setup.

- **Authenticate Button**: This button serves as an additional method to authenticate, complementing the main **Authenticate** button already present in the component.
- **Sign Out Button**: Allows users to sign out when they are currently authenticated.

This button serves a critical role in scenarios where the component cannot retrieve a valid token due to reasons such as:
- The token is invalid.
- The token has expired.
- Other token-related issues.

In such cases, you can use the combo action of signing out and authenticating again to resolve these token-related problems, ensuring seamless authentication management within your application.

<Image src="/img/one_drive/screenshot34.png" alt="alt text" />

### 📝 Inputs

To effectively utilize this component, you need to provide the following input:

1. **userId (`required<string>`):** The ID of the user whose root items you want to list.

<Image src="/img/one_drive/screenshot35.png" alt="alt text" />

> **Note:** With the flexibility and customization in mind, `SmythOS` allows you to expand the default input list by adding new inputs using the `+` button situated in the `Inputs` section.

### 📤 Outputs

After running this component, the following default outputs are generated:

1. **Response:** The raw response from the OneDrive API.
2. **Headers:** HTTP headers detailing the API request-response process.
3. **items:** Parsed list of items retrieved from the OneDrive root directory.

<Image src="/img/one_drive/screenshot36.png" alt="alt text" />

> **Note:** Similar to inputs, you can add new outputs by clicking on the `+` button in the `Outputs` section. This feature can come in handy when you aim to extract specific values from the `Response`. Please refer [here](https://app.smythos.com/doc/#/components/APICall?id=example-without-authentication) to see a practical example.