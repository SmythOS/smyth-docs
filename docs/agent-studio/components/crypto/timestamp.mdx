# Timestamp Component

The `Timestamp` component in `SmythOS` provides the current server timestamp, essential for time synchronization and logging activities.

<Arcade  src="https://demo.arcade.software/mTvAvyGNFG0qRlPddWTr?embed&embed_mobile=tab&embed_desktop=inline&show_copy_link=true" title="Timestamp | SmythOS"/>

---

## Inputs

- **Trigger:** 🔔 Initiates the component's operation. The input value is arbitrary and does not affect the outcome.

---

## Outputs

- **Timestamp:** ⏲️ The server timestamp, formatted as a Unix epoch timestamp in milliseconds (e.g., `1713523863530`).